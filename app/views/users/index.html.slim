h1 ユーザ一覧

table.table.table-striped
  thead
    tr
      th FelicaのIDm
      th 名前
      th グループ名　
      th メールアドレス
      th

  tbody
    - @users.each do |user|
      tr
        td = user.felica.idm
        td = link_to user.name, user_path(user)
        td = user.group.name
        td = user.email
        td = link_to '編集する', edit_felica_user_path(user)
